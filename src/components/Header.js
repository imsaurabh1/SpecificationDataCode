//Header Component

import React from "react";

const Header = () => {
  return (
    <header className="header">
      <div className="logo">Specification Information</div>
    </header>
  );
};

export default Header;
